import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(spacing: 12) {
            Text("GoudEngine")
                .font(.title2.weight(.semibold))
            Text("iOS build pipeline template")
                .foregroundStyle(.secondary)
        }
        .padding()
    }
}
