`AtkinsonHyperlegible-Regular.ttf` is vendored from the official Google Fonts mirror:
`https://github.com/googlefonts/atkinson-hyperlegible`.

License: SIL Open Font License 1.1.

The sandbox uses this font so the cross-language HUD is readable in shipped examples
instead of relying on the tiny internal test font used elsewhere in engine tests.
