// This file is AUTO-GENERATED by GoudEngine codegen. DO NOT EDIT.

import type { IColor, IVec2, IVec3, IRect } from './engine.g.js';

/** RGBA color with float components in 0.0-1.0 range */
export class Color implements IColor {
  constructor(public r: number, public g: number, public b: number, public a: number) {}

  static white(): Color { return new Color(1, 1, 1, 1); }
  static black(): Color { return new Color(0, 0, 0, 1); }
  static red(): Color { return new Color(1, 0, 0, 1); }
  static green(): Color { return new Color(0, 1, 0, 1); }
  static blue(): Color { return new Color(0, 0, 1, 1); }
  static yellow(): Color { return new Color(1, 1, 0, 1); }
  static cyan(): Color { return new Color(0, 1, 1, 1); }
  static magenta(): Color { return new Color(1, 0, 1, 1); }
  static transparent(): Color { return new Color(0, 0, 0, 0); }
  static rgba(r: number, g: number, b: number, a: number): Color { return new Color(r, g, b, a); }
  static rgb(r: number, g: number, b: number): Color { return new Color(r, g, b, 1); }
  static fromHex(hex: number): Color {
    return new Color(((hex >> 16) & 0xff) / 255, ((hex >> 8) & 0xff) / 255, (hex & 0xff) / 255, 1);
  }
  static fromU8(r: number, g: number, b: number, a: number): Color { return new Color(r, g, b, a); }

  withAlpha(a: number): Color { return new Color(this.r, this.g, this.b, a); }
  lerp(other: Color, t: number): Color { return new Color(this.r + (other.r - this.r) * t, this.g + (other.g - this.g) * t, this.b + (other.b - this.b) * t, this.a + (other.a - this.a) * t); }
}

/** 2D floating-point vector */
export class Vec2 implements IVec2 {
  constructor(public x: number, public y: number) {}

  static zero(): Vec2 { return new Vec2(0, 0); }
  static one(): Vec2 { return new Vec2(1, 1); }
  static up(): Vec2 { return new Vec2(0, -1); }
  static down(): Vec2 { return new Vec2(0, 1); }
  static left(): Vec2 { return new Vec2(-1, 0); }
  static right(): Vec2 { return new Vec2(1, 0); }

  add(other: Vec2): Vec2 { return new Vec2(this.x + other.x, this.y + other.y); }
  sub(other: Vec2): Vec2 { return new Vec2(this.x - other.x, this.y - other.y); }
  scale(s: number): Vec2 { return new Vec2(this.x * s, this.y * s); }
  length(): number { return Math.sqrt(this.x * this.x + this.y * this.y); }
  normalize(): Vec2 { const l = this.length(); return l === 0 ? Vec2.zero() : new Vec2(this.x / l, this.y / l); }
  dot(other: Vec2): number { return this.x * other.x + this.y * other.y; }
  distance(other: Vec2): number { return this.sub(other).length(); }
  lerp(other: Vec2, t: number): Vec2 { return new Vec2(this.x + (other.x - this.x) * t, this.y + (other.y - this.y) * t); }
}

/** 3D floating-point vector */
export class Vec3 implements IVec3 {
  constructor(public x: number, public y: number, public z: number) {}

  static zero(): Vec3 { return new Vec3(0, 0, 0); }
  static one(): Vec3 { return new Vec3(1, 1, 1); }
  static up(): Vec3 { return new Vec3(0, 1, 0); }

}

/** Axis-aligned rectangle */
export class Rect implements IRect {
  constructor(public x: number, public y: number, public width: number, public height: number) {}


  contains(p: IVec2): boolean { return p.x >= this.x && p.x <= this.x + this.width && p.y >= this.y && p.y <= this.y + this.height; }
  intersects(o: IRect): boolean { return this.x < o.x + o.width && this.x + this.width > o.x && this.y < o.y + o.height && this.y + this.height > o.y; }
}
