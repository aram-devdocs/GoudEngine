// This file is AUTO-GENERATED by GoudEngine codegen. DO NOT EDIT.
import XCTest
@testable import GoudEngine

final class ErrorTests: XCTestCase {

    func testRecoveryClassValues() {
        XCTAssertEqual(RecoveryClass.recoverable.rawValue, 0)
        XCTAssertEqual(RecoveryClass.fatal.rawValue, 1)
        XCTAssertEqual(RecoveryClass.degraded.rawValue, 2)
    }

    func testGoudErrorDescription() {
        let err = GoudError(
            errorCode: 100,
            message: "test error",
            category: "Resource",
            subsystem: "assets",
            operation: "load",
            recovery: .recoverable,
            recoveryHint: "retry"
        )
        XCTAssertTrue(err.description.contains("Resource"))
        XCTAssertTrue(err.description.contains("100"))
        XCTAssertTrue(err.description.contains("test error"))
    }

    func testGoudErrorIsError() {
        let err = GoudError(
            errorCode: 1,
            message: "msg",
            category: "Context",
            subsystem: "core",
            operation: "init",
            recovery: .fatal,
            recoveryHint: ""
        )
        // GoudError conforms to Error
        let asError: Error = err
        XCTAssertNotNil(asError)
    }

    func testTypedSubclasses() {
        let contextErr = GoudContextError(
            errorCode: 1,
            message: "ctx",
            category: "Context",
            subsystem: "core",
            operation: "create",
            recovery: .fatal,
            recoveryHint: ""
        )
        XCTAssertTrue(contextErr is GoudError)

        let resourceErr = GoudResourceError(
            errorCode: 100,
            message: "res",
            category: "Resource",
            subsystem: "assets",
            operation: "load",
            recovery: .recoverable,
            recoveryHint: "check path"
        )
        XCTAssertTrue(resourceErr is GoudError)
        XCTAssertEqual(resourceErr.errorCode, 100)
    }
}
