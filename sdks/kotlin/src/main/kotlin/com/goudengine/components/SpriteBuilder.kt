// This file is AUTO-GENERATED by GoudEngine codegen. DO NOT EDIT.
package com.goudengine.components

import com.goudengine.internal.SpriteBuilderNative
import com.goudengine.internal.Sprite as JavaSprite

class SpriteBuilder private constructor(private var handle: Long) : AutoCloseable {

    fun withTexture(value: Long): SpriteBuilder {
        handle = SpriteBuilderNative.withTexture(handle, value)
        return this
    }

    fun withColor(r: Float, g: Float, b: Float, a: Float): SpriteBuilder {
        handle = SpriteBuilderNative.withColor(handle, r, g, b, a)
        return this
    }

    fun withAlpha(alpha: Float): SpriteBuilder {
        handle = SpriteBuilderNative.withAlpha(handle, alpha)
        return this
    }

    fun withSourceRect(x: Float, y: Float, width: Float, height: Float): SpriteBuilder {
        handle = SpriteBuilderNative.withSourceRect(handle, x, y, width, height)
        return this
    }

    fun withFlipX(flip: Boolean): SpriteBuilder {
        handle = SpriteBuilderNative.withFlipX(handle, flip)
        return this
    }

    fun withFlipY(flip: Boolean): SpriteBuilder {
        handle = SpriteBuilderNative.withFlipY(handle, flip)
        return this
    }

    fun withFlip(flipX: Boolean, flipY: Boolean): SpriteBuilder {
        handle = SpriteBuilderNative.withFlip(handle, flipX, flipY)
        return this
    }

    fun withZLayer(zLayer: Int): SpriteBuilder {
        handle = SpriteBuilderNative.withZLayer(handle, zLayer)
        return this
    }

    fun withAnchor(x: Float, y: Float): SpriteBuilder {
        handle = SpriteBuilderNative.withAnchor(handle, x, y)
        return this
    }

    fun withCustomSize(width: Float, height: Float): SpriteBuilder {
        handle = SpriteBuilderNative.withCustomSize(handle, width, height)
        return this
    }

    fun build(): Sprite {
        val result = SpriteBuilderNative.build(handle)
        handle = 0L
        return Sprite(result)
    }

    fun free() {
        if (handle != 0L) { SpriteBuilderNative.free(handle); handle = 0L }
    }

    override fun close() = free()

    companion object {
        fun new(textureHandle: Long): SpriteBuilder =
            SpriteBuilder(SpriteBuilderNative.create(textureHandle))

        fun default(): SpriteBuilder =
            SpriteBuilder(SpriteBuilderNative.defaultValue())

    }
}
