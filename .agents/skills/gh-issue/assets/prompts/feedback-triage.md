Working directory: <absolute worktree path>

Requirements:
- Wait for GitHub Claude review on the PR.
- Fix every blocker before proceeding.
- Fix every warning, or reply on the PR with a concrete justification if it should not change.
- Record Claude review status, blocker status, warning status, and justification state in `state.json`.
