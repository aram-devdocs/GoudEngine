Working directory: <absolute worktree path>

Requirements:
- Confirm review gates, PR metadata, Claude review handling, and CI are complete.
- Remove the worktree only after the run is ready for cleanup.
- Mark cleanup complete in `state.json`.
