Working directory: <absolute worktree path>

Requirements:
- Use a conventional PR title.
- Fill the body from `.github/pull_request_template.md`.
- Record PR number, PR title, `template_used=true`, and `template_sections_complete=true` in `state.json`.
- Do not leave required sections empty.
